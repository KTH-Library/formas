# Rename fields to use English field names

These field names match closer to field names used in SweCRIS

## Usage

``` r
rename_fields(x)
```

## Arguments

- x:

  the data frame, usually resulting from a call to fcn formas_projects()
